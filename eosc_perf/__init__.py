"""This module contains the core application start and configuration functionality, and contains all the MVC components
of the application laid out in submodules: controller handles input validation and authorization, model contains the
interface to the database and functionality to store, load, and query data, and view contains all components that
interface with the user."""
