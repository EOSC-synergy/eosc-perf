
To manually test around with the components
1. Run `python model_sandbox.py`
2. A working model is now established, you may interact with it through the interpreter

To launch the webapp entirely:
1. Run `python -m app`
